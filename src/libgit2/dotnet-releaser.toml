# configuration file for dotnet-releaser
# Disable default packs - It will only publish NuGet and the Changelog
[msbuild]
project = "XenoAtom.Interop.libgit2.sln"
[github]
user = "xoofx"
repo = "XenoAtom.Interop.libgit2"